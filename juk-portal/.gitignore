# Lo genérico (node_modules, .next, env, logs, Playwright, .vercel, .trigger…)
# lo cubre el .gitignore de la raíz del workspace. Acá solo lo propio de la app.
.env.local*

# Storage local de documentos (fallback dev sin R2)
.uploads/
